# Optional — protect camera / match write APIs
CAMERA_API_KEY=

# Postgres for walk-up player accounts + shared stats (Railway Postgres)
# Without this, guests and localStorage still work; auth APIs return 503.
DATABASE_URL=

# HMAC secret for httpOnly player session cookies (recommended in production)
SESSION_SECRET=
