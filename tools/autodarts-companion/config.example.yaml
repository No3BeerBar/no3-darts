# Copy to config.yaml

autodarts:
  host: "127.0.0.1"   # PC running Autodarts Board Manager
  port: 3180          # default Board Manager port
  poll_ms: 300        # how often to poll /api/state

no3:
  # Optional — only for `compare` mode
  url: "https://no3-darts-production.up.railway.app"
  room_id: "Board 1"
  camera_api_key: ""

logs_dir: "./logs"
